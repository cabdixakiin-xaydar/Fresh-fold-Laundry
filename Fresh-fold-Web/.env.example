# Leave empty to use the Vite dev proxy (/api → http://127.0.0.1:8000).
# For production builds, set the public API origin (no trailing slash):
# VITE_API_BASE_URL=https://api.yourdomain.com

VITE_API_BASE_URL=
